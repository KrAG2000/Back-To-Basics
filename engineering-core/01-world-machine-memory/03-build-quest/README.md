# Build Quest

Build small experiments that make memory behavior visible.

Suggested quest slices:

- stack vs heap allocation demo
- contiguous vs fragmented access benchmark
- pass-by-value vs pass-by-reference experiment
- object layout inspection
