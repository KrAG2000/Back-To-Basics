# Templates

Store reusable C++ snippets here, for example:

- fast input template
- graph template
- DFS and BFS template
- binary search template
- segment tree template

Keep templates short and trusted so they help during practice instead of becoming another thing to debug.
