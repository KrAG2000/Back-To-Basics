#include <iostream>

#include "dynamic-array/dynamic-array.cpp"
#include "static-array/static-array.cpp"

// Use .h files for header declarations and .cpp files for implementations. This is a common practice in C++ to separate interface from implementation, making the code more organized and easier to maintain.
// In future, use .h. For now, we are using .cpp for simplicity and to avoid issues with multiple inclusions.

int main() {
    // Testing StaticArray & DynamicArray classes
}
